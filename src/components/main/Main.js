import React from 'react';

function Main() {
    return (
        <div>
            <button> Add User </button>
            <button> Remove User </button>
        </div>
    );
}

export default Main;